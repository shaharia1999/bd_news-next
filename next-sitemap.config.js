/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: 'https://www.newsus.shop', // No trailing slash at the end
  generateRobotsTxt: true, // Also generates robots.txt
  sitemapSize: 5000, // Optional, split if many URLs
};
